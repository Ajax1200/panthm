name: PANTHM Autopilot Blogging

on:
  schedule:
    # Trigger runs once daily at 4:00 AM UTC (9:30 AM IST)
    - cron: '0 4 * * *'
  workflow_dispatch: # Enable running manual workflow executions from GitHub UI

# Prevent parallel runs — if a new run is queued while one is already active,
# the queued run will wait. This is the primary defence against duplicate posts.
concurrency:
  group: autopilot-blogging
  cancel-in-progress: false

jobs:
  run-autopilot:
    runs-on: ubuntu-latest
    # Hard kill at 330 min — safely under GitHub's 360-min wall, leaving 30 min for cleanup
    timeout-minutes: 330

    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: Setup Node.js environment
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm ci || npm install

      - name: Run Autopilot blogging engine
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.PAT_GITHUB_TOKEN }}
          GITHUB_REPO: ${{ github.repository }}
          GITHUB_SITEMAP_PATH: 'public/sitemap.xml'
          GITHUB_BRANCH: 'main'
          LINKEDIN_ACCESS_TOKEN: ${{ secrets.LINKEDIN_ACCESS_TOKEN }}
          LINKEDIN_MEMBER_ID: ${{ secrets.LINKEDIN_MEMBER_ID }}
          LINKEDIN_CLIENT_ID: ${{ secrets.LINKEDIN_CLIENT_ID }}
          LINKEDIN_CLIENT_SECRET: ${{ secrets.LINKEDIN_CLIENT_SECRET }}
          LINKEDIN_MAKE_WEBHOOK_URL: ${{ secrets.LINKEDIN_MAKE_WEBHOOK_URL }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: |
          node automation/autopilot.js
